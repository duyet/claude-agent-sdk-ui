# Claude API (Official Anthropic API)
ANTHROPIC_API_KEY=your-anthropic-api-key

# ZAI API
ZAI_API_KEY=your_zai_api_key_here
ZAI_BASE_URL=https://api.z.ai/api/anthropic

# Minimax API
MINIMAX_API_KEY=your_minimax_api_key_here
MINIMAX_BASE_URL=your_minimax_base_url_here

# Proxy API (local claude-code-proxy)
PROXY_BASE_URL=http://localhost:4000/openai:nvidia/nemotron-3-nano-30b-a3b
# PROXY_BASE_URL=http://localhost:4000/ollama:gpt-oss:20b

# ==============================================================================
# SECURITY CONFIGURATION
# ==============================================================================
# IMPORTANT: Security best practices for API keys:
#   - Never commit real API keys to version control
#   - Use strong, unique keys (recommend: `openssl rand -hex 32`)
#   - In production, use a secrets manager (e.g., AWS Secrets Manager, HashiCorp Vault)
# ==============================================================================

API_KEY=change-me-use-strong-key

# CORS Configuration
# In production, set explicit origins for your frontend domain(s) only.
# Avoid using wildcards (*) in production environments.
CORS_ORIGINS=http://localhost:7002,https://claude-agent-sdk-chat.tt-ai.org

API_HOST=0.0.0.0
API_PORT=7001

# Storage Configuration
# Directory for session and history data (defaults to backend/data)
# DATA_DIR=/path/to/data
