# ==============================================================================
# API CONFIGURATION (Server-Side Only - NOT exposed to browser)
# ==============================================================================
BACKEND_API_URL=https://claude-agent-sdk-fastapi-sg4.tt-ai.org/api/v1

# API Key (REQUIRED) - used by proxy routes and JWT derivation
# Generate with: openssl rand -hex 32
API_KEY=your-api-key-here

# NOTE: JWT tokens are created locally using secret derived from API_KEY

# ==============================================================================
# PUBLIC CONFIGURATION (Exposed to Browser)
# ==============================================================================
NEXT_PUBLIC_WS_URL=wss://claude-agent-sdk-fastapi-sg4.tt-ai.org/api/v1/ws/chat
